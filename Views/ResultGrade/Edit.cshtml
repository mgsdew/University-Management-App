@model UniversityManagementApp.Models.ResultGrade

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ResultGrade</legend>

        @Html.HiddenFor(model => model.ResultGradeId)

        <div class="editor-label">
            @Html.LabelFor(model => model.GradeLater)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.GradeLater)
            @Html.ValidationMessageFor(model => model.GradeLater)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.GradeValue)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.GradeValue)
            @Html.ValidationMessageFor(model => model.GradeValue)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
