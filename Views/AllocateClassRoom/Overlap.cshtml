@model IEnumerable<UniversityManagementApp.Models.AllocateClassRoom>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Department.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Course.Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClassRoom.RoomNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WeekDay.Day)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StarTimeHour)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.EndTimeHour)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Department.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClassRoom.RoomNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WeekDay.Day)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StarTimeHour)
                :
                @Html.DisplayFor(modelItem => item.StarTimeMin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndTimeHour)
                :
                @Html.DisplayFor(modelItem => item.EndTimeMin)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.AllocateClassRoomId }) |
                @Html.ActionLink("Details", "Details", new { id = item.AllocateClassRoomId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.AllocateClassRoomId })
            </td>
        </tr>
    }

</table>
